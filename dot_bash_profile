# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
   . ~/.bashrc
fi

# User specific environment and startup programs
if [ "$REMOTE_USER" = "Lunar-Escapist9" ] && type zsh >/dev/null 2>&1; then exec zsh; fi
